/**
 * Hero block.
 *
 * Server-rendered by render.php via ACF (`acf.mode: preview`). This entry exists
 * only so @wordpress/scripts compiles the block's stylesheet — there is no
 * editor JavaScript.
 */
import './style.scss';
